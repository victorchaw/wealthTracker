version: "3.8"
services:
  ocr-service:
    build: .
    ports:
      - "3000:3000"
    environment:
      - LLAMAPARSE_API_KEY=${LLAMAPARSE_API_KEY}
      - PORT=3000
      - MAX_FILE_SIZE=10485760
    volumes:
      - ./logs:/app/logs
